import { product } from './product';
import { clientSector } from './clientSector';
import { fountainProject } from './fountainProject';
import { installation } from './installation';
import { siteSettings, address } from './siteSettings';

export const schemaTypes = [
  siteSettings,
  address,
  product,
  clientSector,
  fountainProject,
  installation,
];
